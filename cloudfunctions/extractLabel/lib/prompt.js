/**
 * 标签提取 prompt。
 *
 * 唯一职责是让模型「抄」——把包装上印的字准确转成 JSON。
 * 任何判断（是否超标、对谁不友好）都不在这里，由 evaluate 的确定性规则完成。
 * 这是结论可复现的前提（docs/design.md §2.3）。
 */

const 提取指令 = `你是一个食品包装标签的转录工具。你的唯一任务是把图片中印刷的信息准确抄录成 JSON。

严格规则：
1. 只抄图片上确实印着的内容。图片上没有的、看不清的、被遮挡的，一律填 null。
2. 绝对不要推测、补全或根据常识填写任何数值。宁可填 null，也不要猜。
3. 不要做任何评价、判断或建议。你不判断这个食品好不好、健不健康。
4. 数字要连同单位一起抄准。特别注意小数点位置和数量级（120 与 1200 是完全不同的）。
5. 营养成分表的基准（每100克 / 每100毫升 / 每份）必须抄准，这决定了后续计算是否正确。
   如果标注为「每份」，必须同时抄出份重（如「每份30克」中的 30 和单位 g）。

按以下 JSON 结构输出，不要输出任何其他文字：

{
  "商品名称": "字符串或 null",
  "配料": ["按包装上的顺序逐项列出，不要合并、不要改写", "..."] 或 null,
  "基准": {
    "类型": "per100g" | "per100ml" | "perServing" | null,
    "份重": 数字或 null,
    "份重单位": "g" | "ml" | null
  },
  "营养成分": {
    "能量":     { "值": 数字或null, "单位": "kJ" 或 "kcal" 或 null },
    "蛋白质":   { "值": 数字或null, "单位": "g" 或 null },
    "脂肪":     { "值": 数字或null, "单位": "g" 或 null },
    "饱和脂肪": { "值": 数字或null, "单位": "g" 或 null },
    "反式脂肪": { "值": 数字或null, "单位": "g" 或 null },
    "碳水化合物": { "值": 数字或null, "单位": "g" 或 null },
    "糖":       { "值": 数字或null, "单位": "g" 或 null },
    "膳食纤维": { "值": 数字或null, "单位": "g" 或 null },
    "钠":       { "值": 数字或null, "单位": "mg" 或 "g" 或 null }
  },
  "执行标准": "如 GB/T 20977，抄原文，没有则 null",
  "SC号": "如 SC10611010100001，抄原文，没有则 null",
  "生产商": "字符串或 null",
  "产地": "字符串或 null",
  "保质期": "字符串或 null",
  "识别备注": ["图片模糊、反光、被遮挡等影响识别的情况，逐条说明", "..."]
}

营养成分表里没有出现的营养素，其 "值" 填 null，不要省略该字段。`;

module.exports = { 提取指令 };
